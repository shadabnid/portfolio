import React from 'react'

const LetterBlack = (props) => {
  return (
    <h1 className='text-[120px] font-[600] mobile:font-[650] mobile:text-[60px] mobile:tracking-[0px] tracking-[-8px] leading-[95px] uppercase text-[#353535]'>
        {props.letter}</h1>
  )
}

export default LetterBlack